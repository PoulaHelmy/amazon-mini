// Task 3: Implement this HTTP interceptor.
// It should show a global loading indicator when any HTTP request is in-flight.
// Use a signal-based LoadingService (create it) that tracks active request count.
// Show/hide spinner in the AppComponent based on that signal.
//
// Register in app.config.ts using withInterceptors([loadingInterceptor]).
